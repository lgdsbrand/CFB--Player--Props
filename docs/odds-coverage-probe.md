# Odds coverage probe — The Odds API / NCAAF player props

Generated by `python -m worker.jobs.probe_odds` at 2026-08-24 16:47 UTC.
Historical probe date: `2025-10-18T18:00:00Z`.

Answers the three questions CLAUDE.md §9.1 leaves open: does this plan
cover NCAAF player props, what does a weekly refresh cost, and can edge
be backtested against historical lines.

## Findings

### key source — INFO

read from ODDS_API_KEY_FREE

### plan / auth — PASS

key valid, americanfootball_ncaaf present (active=True). 427 credits remaining of 500; last call cost 0

- allowance totals 500 credits, which indicates a FREE plan — coverage findings below are only valid for that tier

### live events — PASS

111 upcoming event(s); soonest 2026-08-29 16:00:00+00:00. 427 credits remaining of 500; last call cost 0

### live player props — PASS

probed 20 event(s); markets returned data

- books returning data: 1 (fanduel)
- markets seen: 5/9 (player_pass_tds, player_pass_yds, player_reception_yds, player_receptions, player_rush_yds) — a FLOOR on coverage, not a measurement of it
- markets not seen in this sample (4): player_anytime_td, player_pass_attempts, player_pass_completions, player_rush_attempts — absence here is not proof of absence on the plan; widen the sample before concluding
- prices: 35 two-way, 0 one-sided (0% one-sided)
- one-sided prices CANNOT be de-vigged and yield no edge — this percentage is the ceiling on edge coverage FOR THE MARKET MIX above, not for the plan: anytime TD is posted Yes-only by most books and will dominate any sample it appears in, so read this per market rather than as one headline rate
- measured cost: 1 credits/event -> ~57 credits for a 60-game slate, per refresh

### historical odds — INFO

skipped by --skip-historical

## Market mapping under test

| our `markets.key` | The Odds API key |
|---|---|
| `anytime_td` | `player_anytime_td` |
| `pass_attempts` | `player_pass_attempts` |
| `pass_completions` | `player_pass_completions` |
| `pass_tds` | `player_pass_tds` |
| `pass_yards` | `player_pass_yds` |
| `rec_yards` | `player_reception_yds` |
| `receptions` | `player_receptions` |
| `rush_attempts` | `player_rush_attempts` |
| `rush_yards` | `player_rush_yds` |
