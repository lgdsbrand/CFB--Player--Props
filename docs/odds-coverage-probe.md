# Odds coverage probe — The Odds API / NCAAF player props

Generated by `python -m worker.jobs.probe_odds` at 2026-08-03 01:52 UTC.
Historical probe date: `2025-10-18T18:00:00Z`.

Answers the three questions CLAUDE.md §9.1 leaves open: does this plan
cover NCAAF player props, what does a weekly refresh cost, and can edge
be backtested against historical lines.

## Findings

### key source — INFO

read from ODDS_API_KEY

### plan / auth — PASS

key valid, americanfootball_ncaaf present (active=True). 15,941 credits remaining of 20,000; last call cost 0

- allowance totals 20,000 credits, which indicates a PAID plan — coverage findings below are only valid for that tier

### live events — PASS

126 upcoming event(s); soonest 2026-08-29 16:00:00+00:00. 15,941 credits remaining of 20,000; last call cost 0

### live player props — INFO

probed 1 event(s); no props posted yet — UNRESOLVED, re-run within 7 days of kickoff

- nearest probed game is 27 days away — books do not post player props this far out, so an empty response here is expected and says NOTHING about whether this plan covers them
- books returning data: 0 (none)
- markets seen: 0/9 (none) — a FLOOR on coverage, not a measurement of it
- markets not seen in this sample (9): player_anytime_td, player_pass_attempts, player_pass_completions, player_pass_tds, player_pass_yds, player_reception_yds, player_receptions, player_rush_attempts, player_rush_yds — absence here is not proof of absence on the plan; widen the sample before concluding
- measured cost: 0 credits/event -> ~0 credits for a 60-game slate, per refresh
- the call returned 200 rather than 401/403, so the event-odds endpoint itself is reachable on this key

### historical odds — PASS

available. 52 event(s) at 2025-10-18T18:00:00Z. 15,940 credits remaining of 20,000; last call cost 1

- 40 of 52 had not yet kicked at the snapshot; only those can carry props

### historical player props — PASS

2 upcoming event(s) at 2025-10-18T18:00:00Z

- probed 2 upcoming event(s); 1 carried player props
- books returning data: 4 (betonlineag, bovada, draftkings, williamhill_us)
- markets seen: 2/9 (player_anytime_td, player_rush_attempts) — a FLOOR on coverage, not a measurement of it
- markets not seen in this sample (7): player_pass_attempts, player_pass_completions, player_pass_tds, player_pass_yds, player_reception_yds, player_receptions, player_rush_yds — absence here is not proof of absence on the plan; widen the sample before concluding
- prices: 4 two-way, 60 one-sided (94% one-sided)
- one-sided prices CANNOT be de-vigged and yield no edge — this percentage is the ceiling on edge coverage FOR THE MARKET MIX above, not for the plan: anytime TD is posted Yes-only by most books and will dominate any sample it appears in, so read this per market rather than as one headline rate
- measured cost: 20 credits total — ~20 for an event carrying 2 market(s); events without props cost 0
- 1 of 2 probed event(s) carried NO props, so a backfill pays only for the games books actually priced — measure the carry rate over a full slate before budgeting one
- EDGE IS BACKTESTABLE

## Market mapping under test

| our `markets.key` | The Odds API key |
|---|---|
| `anytime_td` | `player_anytime_td` |
| `pass_attempts` | `player_pass_attempts` |
| `pass_completions` | `player_pass_completions` |
| `pass_tds` | `player_pass_tds` |
| `pass_yards` | `player_pass_yds` |
| `rec_yards` | `player_reception_yds` |
| `receptions` | `player_receptions` |
| `rush_attempts` | `player_rush_attempts` |
| `rush_yards` | `player_rush_yds` |
