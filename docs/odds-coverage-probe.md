# Odds coverage probe — The Odds API / NCAAF player props

Generated by `python -m worker.jobs.probe_odds` at 2026-07-31 19:24 UTC.
Historical probe date: `2025-10-18T18:00:00Z`.

Answers the three questions CLAUDE.md §9.1 leaves open: does this plan
cover NCAAF player props, what does a weekly refresh cost, and can edge
be backtested against historical lines.

## Findings

### key source — INFO

read from ODDS_API_KEY

### plan / auth — PASS

key valid, americanfootball_ncaaf present (active=True). 0 credits remaining of 20,000; last call cost 0

- allowance totals 20,000 credits, which indicates a PAID plan — coverage findings below are only valid for that tier

### live events — PASS

126 upcoming event(s); soonest 2026-08-29 16:00:00+00:00. 0 credits remaining of 20,000; last call cost 0

### live player props — INFO

UNRESOLVED — account is out of credits, so nothing was measured. 0 credits remaining of 20,000; last call cost 0

- This is a QUOTA response (OUT_OF_USAGE_CREDITS), not an entitlement one. It says nothing about what this plan covers.
- Re-run after the monthly reset.

### historical odds — INFO

UNRESOLVED — account is out of credits, so nothing was measured. 0 credits remaining of 20,000; last call cost 0

- This is a QUOTA response (OUT_OF_USAGE_CREDITS), not an entitlement one. It says nothing about what this plan covers.
- Re-run after the monthly reset.

## Market mapping under test

| our `markets.key` | The Odds API key |
|---|---|
| `anytime_td` | `player_anytime_td` |
| `pass_attempts` | `player_pass_attempts` |
| `pass_completions` | `player_pass_completions` |
| `pass_tds` | `player_pass_tds` |
| `pass_yards` | `player_pass_yds` |
| `rec_yards` | `player_reception_yds` |
| `receptions` | `player_receptions` |
| `rush_attempts` | `player_rush_attempts` |
| `rush_yards` | `player_rush_yds` |
